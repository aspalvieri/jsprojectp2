extends ../layouts/main.pug
block content
    .container
        header
            h1= title
            small= task.status

        div
            = task.description || "Your task is processing..."